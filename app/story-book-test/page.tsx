import { StoryBookTestShell } from "@/components/story-book-test/story-book-test-shell";

export const metadata = {
  title: "Story Book Test — CrayonSparks",
  robots: { index: false, follow: false },
};

export default function StoryBookTestPage() {
  return <StoryBookTestShell />;
}
